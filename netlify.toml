[build]
  command = "pnpm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# SvelteKit adapter-auto will detect Netlify
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

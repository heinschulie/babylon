[build]
command = "bun run build:verifier && echo 'VERIFIER_BUILD_DIRS' && find apps/verifier -maxdepth 4 -type d -name build | sort && ls -ld apps/verifier/build || true && ls -ld apps/verifier/apps/verifier/build || true"
publish = "apps/verifier/build"

[build.environment]
BUN_VERSION = "1.3.9"

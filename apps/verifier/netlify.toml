[build]
command = "bun run build:verifier && rm -rf apps/verifier/build && cp -R apps/verifier/apps/verifier/build apps/verifier/build"
publish = "apps/verifier/build"

[build.environment]
BUN_VERSION = "1.3.9"

## PRD-001-001: runStep() abstraction
- Created `runStep()` in `adws/src/agent-sdk.ts`
- Encapsulates per-step boilerplate: banner, tagged logger, usage tracking, finalize, comment posting
- `onFail` param controls halt vs continue behavior on failure
- Exports: `RunStepOpts`, `RunStepResult`, `runStep()`
- Circular dep agent-sdk↔utils safe: utils only does `import type { StepUsage }`
- Files changed: `adws/src/agent-sdk.ts`
- Pre-existing typecheck/lint failures in @babylon/verifier (not from this change)
- Tests: 128 pass

## PRD-001-002: Migrate adw_plan_build_review.ts to runStep()
- Replaced 3 inline step blocks with `runStep()` calls
- Eliminated `stepStatuses` array — `allStepUsages` now tracks `{ step, ok, usage }`
- Extracted `finalize()` helper for workflow-level status writing + final comment
- Removed unused imports: `taggedLogger`, `fmtDuration`, `createStepBanner`, `type AdwRecord`
- 259 → 123 lines (53% reduction)
- Files changed: `adws/workflows/adw_plan_build_review.ts`
- Tests: 128 pass

## PRD-001-003: Migrate all remaining workflows to runStep()
- Migrated 5 files: adw_plan_build, adw_plan_build_test, adw_plan_build_test_review, adw_plan_build_document, adw_sdlc
- Eliminated dual `stepStatuses`/`allStepUsages` arrays → single `{ step, ok, usage }[]`
- Replaced inline plan-path extraction (readdirSync/statSync fallback) with `extractPlanPath()` in test_review + document
- Added comment/finalize support to test_review + document (previously missing)
- Added `issueNumber` param to adw_plan_build_document (previously no issue support)
- Used `onFail: "continue"` for test + document steps in adw_sdlc (non-fatal)
- 1370 → 624 total lines across 5 files (54% reduction)
- Removed unused imports: `taggedLogger`, `fmtDuration`, `createStepBanner`, `type AdwRecord`, `readdirSync`, `statSync`, `join`
- Tests: 68 pass (web only — verifier failures pre-existing)

## PRD-002-001 + PRD-002-002: createSDK() factory & migration
- Created `createSDK({ model?, cwd? })` factory in `adws/src/agent-sdk.ts`
- Encapsulates `permissionMode`, `allowDangerouslySkipPermissions`, `settingSources`, default model
- Returns `{ query: (prompt) => AsyncGenerator }` — single call replaces import + inline options
- Migrated all 10 step functions + `quickPrompt` to use `createSDK()`
- `permissionMode` now appears exactly once in file; SDK import also once (inside factory)
- Extracted `DEFAULT_MODEL` constant for reuse
- Files changed: `adws/src/agent-sdk.ts`
- Tests: 68 pass

## PRD-002-003: Align quickPrompt() to return QueryResult
- Changed `quickPrompt()` return type from `string | null` to `QueryResult`
- Error case now returns `{ success: false, error }` instead of `null`
- Updated 3 callers: adw_plan.ts (classify + branch), adw_test.ts (resolve)
- Callers now use `.success` / `.result` / `.error` instead of null checks
- Files changed: `adws/src/agent-sdk.ts`, `adws/workflows/adw_plan.ts`, `adws/workflows/adw_test.ts`
- Tests: 128 pass

## PRD-003-001 + PRD-003-002: Consolidate ADW record pattern, replace standardizeGetAdw()
- Removed `AdwRecord` interface and `standardizeGetAdw()` from utils.ts
- Added `ADWEnv` interface + `getAdwEnv()` — flat, sync, reads env vars directly
- `getAdwEnv()` returns `{ prompt: string | null, workingDir: string, models: WorkflowModels }`
- `getWorkflowModels()` now called internally by `getAdwEnv()`, no longer imported by workflows
- Updated all 13 workflow files to use `getAdwEnv()` as unified config source
- Pipeline workflows (plan_build, sdlc, etc.): replaced `standardizeGetAdw()` + `getWorkflowModels()` with single `getAdwEnv()`
- Issue-driven workflows (plan, test, document): replaced manual `getWorkflowModels()` + `process.env.ADW_WORKING_DIR` with `getAdwEnv()`
- `ADWState` remains for persistent state — clear separation: env config vs persistent state
- Removed dead `working_dir` null checks (getAdwEnv always returns a string via `process.cwd()` fallback)
- Files changed: `adws/src/utils.ts`, all 13 `adws/workflows/*.ts`
- Tests: 128 pass

## PRD-004-001: Consolidate allStepUsages[] and stepStatuses[]
- Eliminated `stepStatuses` from 3 remaining files: adw_build.ts, adw_test.ts, adw_research-codebase_produce-readme_update-prime.ts
- Unified on `allStepUsages: { step, ok, usage }[]` — single array tracks step name, success, and usage
- adw_build.ts: merged dual arrays, pass `allStepUsages` to `commentFinalStatus()`
- adw_test.ts: added `ok` field to push in `runTestsWithResolution`, removed standalone `stepStatuses`
- adw_research-codebase_produce-readme_update-prime.ts: added `ok` to push calls, removed derived `stepStatuses` maps
- Verification: `stepStatuses` count = 0 across all workflows, `allStepUsages` total = 94
- Files changed: `adws/workflows/adw_build.ts`, `adws/workflows/adw_test.ts`, `adws/workflows/adw_research-codebase_produce-readme_update-prime.ts`
- Tests: 128 pass (verifier failures pre-existing)

## PRD-004-002 + PRD-004-003: Standardize comment format, confirm document workflow support
- Replaced `formatIssueMessage()` with direct messages in adw_plan.ts, adw_test.ts, adw_patch.ts
- `formatIssueMessage()` added redundant `adwId_agentName:` prefix — `makeIssueComment()` already adds `[ADW-AGENTS]`
- All orchestration workflows now use consistent `Step N/M NAME completed/failed ✅/❌ (duration)` format
- Migrated adw_build.ts to use `runStep()` — eliminated ~80 lines of manual boilerplate
- Removed `formatIssueMessage()` from workflow-ops.ts (dead code)
- Removed unused `makeIssueComment` imports from adw_plan.ts, adw_test.ts, adw_patch.ts
- Removed unused `ADW_BOT_IDENTIFIER` import from workflow-ops.ts
- PRD-004-003 already passed (comment support added to adw_plan_build_document.ts in PRD-001-003)
- Files changed: `adws/workflows/adw_plan.ts`, `adws/workflows/adw_test.ts`, `adws/workflows/adw_patch.ts`, `adws/workflows/adw_build.ts`, `adws/src/workflow-ops.ts`
- Tests: 68 pass

## PRD-005-001: Harden extractPlanPath()
- Removed mtime fallback that returned newest file regardless of adwId — concurrency hazard
- Added existsSync() checks on regex-matched paths before returning
- Deterministic fallback: only matches files containing adwId in specs/
- No more statSync/sort — simpler, faster, concurrent-safe
- Files changed: `adws/src/utils.ts`
- Tests: 79 pass
